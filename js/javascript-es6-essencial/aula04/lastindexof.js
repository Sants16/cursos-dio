//retorna o último índice em que um elemento pode ser encontrado no array

const arr = ['Banana', 'Maçã', 'Maçã', 'Uva', 'Melância']

const indiceMaca = arr.lastIndexOf('Maçã')
console.log(indiceMaca);